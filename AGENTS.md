# Agent 工作约束

本仓库是一个 Obsidian 知识库。Agent 整理笔记时必须遵守以下规则。

## Git 分支

- Agent 开始工作前确认当前分支，默认直接在主分支（当前为 `master`）上修改和提交。
- 只有用户明确要求时才创建或使用独立工作分支；否则不创建 `agent/*` 分支。
- 如果会话开始时已在非主分支，先检查工作区和分支差异，在保留用户未提交修改的前提下将已有工作合并回 `master`，再继续工作。

## 目录职责

- `02 Source notes/`：用户提供的原始笔记。默认只读，不直接改写、删除或移动其中内容。
- `03 Wiki notes/`：从 Source notes 整理出的独立知识笔记。
- `04 MOC/`：通过 wikilinks 串联 Wiki notes 的地图笔记（MOC）。

## 修改前审批

每次准备整理或修改笔记时，必须先提交修改计划，等待用户明确批准后再执行。计划至少包括：

- 整理目标；
- 计划新增、修改或删除的文件；
- 每个文件的具体变更；
- 需要用户确认的语义、公式、链接或拆分问题。

未获得明确批准前，不得修改文件。执行前应检查工作区状态，避免覆盖用户已有的未提交修改。

## Source notes 到 Wiki notes

- 将 Source notes 中的各个知识点原样、完整地拆分为 Wiki notes；不得遗漏、擅自合并或改变原意。
- 默认以一个相对独立的知识点对应一个 Wiki note；如果拆分边界不明确，应在计划中说明并请求确认。
- 可以调整标题、层级、段落顺序和表达结构，以形成可阅读的知识单元，但不能凭空添加未经依据的结论。
- 优先复用已有 Wiki notes，避免创建重复笔记。
- Wiki note 的文件名应准确反映主题，并遵循仓库已有的命名风格。
- 每篇 Wiki note 尽量以定义型句子开头，例如：
  - `xxx 是 xxx。`
  - `xxx 指 xxx。`
  - `xxx 表示 xxx。`
- 如果 Wiki note 没有合适的定义型开头，不要擅自强行补写。应在反馈中明确指出该问题，并给出可供用户修改的定义建议。

## MOC

- 为整理出的 Wiki notes 创建或更新对应的 `04 MOC/` 笔记。
- MOC 使用 Obsidian wikilink 串联笔记，例如 `[[笔记标题]]`。
- 只链接实际存在或本次计划创建的 Wiki notes，不创建失效链接。
- 优先更新已有相关 MOC，避免创建重复 MOC。

## 排版、校对与链接

整理每篇 Wiki note 时必须检查：

- 不应存在和文件名相同的一级标题
- 英文与中文之间添加一个空格；
- 中文与行内 LaTeX 公式之间添加一个空格；
- 中文句子中的字母、变量、数学符号等公式元素使用 LaTeX 包裹，例如 `$x$`、`$f(x)$`；
- 参考链接使用正确的 Markdown 语法：`[链接文字](https://example.com)`；
- 检查链接 URL、链接文字及 Markdown 括号是否完整；
- 检查并修正明显的错别字；
- 保持原始知识点和语义不变。

排版或错别字问题可以直接修正，并在完成报告中说明具体改动。若修正可能改变语义，必须先列入待确认事项，不得自行决定。

## LaTeX 占位符

- 识别用户笔记中的 LaTeX 占位符，并在语义明确时完成公式。
- 公式应使用规范的 LaTeX 语法，并保持与上下文一致。
- 中文中的公式元素应使用行内或块级 LaTeX 包裹，不能裸写造成歧义。
- 如果公式含义、变量定义或符号无法可靠判断，不得猜测；应保留占位内容并向用户说明疑点。

## 不确定内容与反馈

以下情况必须反馈用户，不得擅自决定：

- 知识点拆分边界不明确；
- 可能存在多种语义解释；
- 公式或 LaTeX 占位符无法确定；
- 参考链接目标不明确或链接已失效；
- 修正内容可能改变原意；
- Wiki note 缺少定义型开头。

反馈应区分：

- **已直接修正**：排版、明显错别字、明确的链接语法问题，并列出修改内容；
- **待用户修改**：缺少定义型开头，并提供建议句子；
- **待用户确认**：语义、公式、拆分或链接存在不确定性。

## 完成报告

执行完成后必须报告：

1. 新增、修改和删除的文件；
2. Source note 到 Wiki note 的拆分结果；
3. 新增或更新的 MOC 及其链接；
4. 直接修正的排版、错别字和链接问题；
5. 缺少定义型开头的 Wiki notes 及修改建议；
6. 尚未解决、需要用户确认的问题。
