假设 $B$ 坐标系中的点 $\mathbf{p}_B$，要映射到 $A$ 坐标系，计算步骤可以写作：

$$
\mathbf{p}_A = \mathbf{R}_{AB} \cdot \mathbf{p}_B + \mathbf{t}_{AB}
$$

这个变换被称为[[仿射变换]]（其中的旋转是线性变换，我们可以直接通过旋转矩阵左乘 $\mathbf{p}_B$ 得到。但是平移不是线性变换）。

通过构建[[齐次变换矩阵]]可以将仿射变换弄成高一维度空间的线性变换，进而可以用一个矩阵乘法来进行变换。

## 来源

- [[三维坐标系变换]]
