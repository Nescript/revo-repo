**** # 现代控制理论

来源：[如何入门现代控制理论](https://zhuanlan.zhihu.com/p/57051153)

## 特征值和特征向量

考虑一个表示线性变换的矩阵。在变换作用下，大多数向量会离开原来由自身张成的空间。但有些向量比较特殊，变换作用在它们身上时，不会使它们离开自身张成的空间，而只相当于按某个比例进行缩放。

这些特殊的向量称为特征向量。这里的关键是特征向量所张成的空间：特征向量经过线性变换后，仍然位于原来由它张成的空间中。

具有相同特征值的特征向量与零向量构成的集合称为特征空间。特征值就是特征向量在变换中被缩放的比例。

形式化地写就是：

$$
Av=\lambda v
$$

详见：[[特征值和特征向量]]。

## 相似对角化

利用特征值和特征向量，可以将矩阵进行相似对角化。

设 $P$ 的列向量是 $A$ 的特征向量：

$$
P=\begin{bmatrix}v_1&v_2&\cdots&v_n\end{bmatrix}
$$

对应的特征值为 $\lambda_1,\lambda_2,\dots,\lambda_n$，并令：

$$
D=\operatorname{diag}(\lambda_1,\lambda_2,\dots,\lambda_n)
$$

由于

$$
Av_i=\lambda_i v_i
$$

把这些等式按列合并，可以得到：

$$
AP=PD
$$

再因为 $P$ 可逆，两边左乘 $P^{-1}$：

$$
P^{-1}AP=P^{-1}PD=D
$$

所以：

$$
\boxed{P^{-1}AP=D}
$$

也就是说，对角矩阵 $D$ 是矩阵 $A$ 在特征向量坐标系下的表示。

详见：[[相似对角化]]。

## 矩阵换基与相似变换

$P^{-1}AP$ 的实质是把变换 $A$ 换到特征向量坐标系中表示。

假设一个向量在新坐标系中的坐标是 $x'$。

### 1. 用 $P$ 换回旧坐标

$$
x=Px'
$$

### 2. 在旧坐标系中使用矩阵 $A$

$$
y=Ax=A(Px')
$$

### 3. 把结果换回新坐标

$$
y'=P^{-1}y
$$

代入前面的式子：

$$
y'=P^{-1}APx'
$$

因此，在新坐标系中，矩阵 $A$ 对应的矩阵就是：

$$
P^{-1}AP
$$

所以，$P^{-1}AP$ 的含义是：输入坐标先由新坐标换成旧坐标，经过 $A$ 变换后，再换回新坐标。

详见：[[矩阵换基与相似变换]]。

# 特征函数和算子

前面矩阵的特征向量满足：

$$
Av=\lambda v
$$

如果把 $A$ 看作某种算子，把 $v$ 换成函数，就可以得到特征函数的概念。详见：[[特征函数]]。

## 算子

算子表示对某类数学对象进行运算的映射，输入和输出往往是向量、函数或其他对象。

例如微分算子：

$$
D[f]=f'
$$

它把一个函数映射为另一个函数：

$$
D:\{\text{可导函数}\}\to\{\text{函数}\}
$$

积分算子也是算子：

$$
T[f](x)=\int_0^x f(t)\,\mathrm{d}t
$$

算子可以说是一种特殊的映射。矩阵也可以看作一种算子，它把向量变成另一个向量。

详见：[[算子]]。

## 求导算子的特征函数

回到前面的特征值方程，将 $A$ 从矩阵算子换成其他算子，例如微分算子 $\frac{\mathrm{d}}{\mathrm{d}t}$，则有：

$$
\frac{\mathrm{d}x(t)}{\mathrm{d}t}=\lambda x(t)
$$

两边同乘 $\frac{\mathrm{d}t}{x(t)}$：

$$
\frac{\mathrm{d}x(t)}{x(t)}=\lambda\,\mathrm{d}t
$$

两边积分：

$$
\int\frac{1}{x(t)}\,\mathrm{d}x(t)=\int\lambda\,\mathrm{d}t
$$

得到：

$$
\ln|x(t)|=\lambda t+C
$$

两边取指数：

$$
 x(t)=e^{\lambda t+C}=e^C e^{\lambda t}
$$

其中的常数 $e^C$ 在 $t=0$ 时满足：

$$
x(0)=e^C
$$

综上所述：

$$
x(t)=x(0)e^{\lambda t}
$$

我们通过求解特征值方程找到了微分算子自然存在的特征函数 $e^{\lambda t}$。

这里的 $x(0)$ 可以理解为特征函数的系数。类似于同一个特征空间中有无数个向量，但它们都可以表示为同一方向上的向量，特征函数前面的系数是多少并不妨碍其性质，即：

$$
\frac{\mathrm{d}}{\mathrm{d}t}\left(e^{\lambda t}\right)=\lambda e^{\lambda t}
$$

运算的结果体现为幅值变化。

## 相关 Wiki notes

- [[特征值和特征向量]]
- [[相似对角化]]
- [[矩阵换基与相似变换]]
- [[算子]]
- [[特征函数]]


回到刚才 
对 $x(t)$ 的数量进行拓展，可以得到：
$$
x(t)=
\begin{bmatrix}
x_1 \\
x_2 \\
\vdots \\
x_t
\end{bmatrix}
$$
$$Ax(t)=\lambda x(t)$$将A换为微分算子后
$$\dot{x}(t)=\lambda x(t)$$
展开得到：$$\frac{d}{dt} \begin{bmatrix} x_1(t)\\ x_2(t)\\ \vdots\\ x_n(t) \end{bmatrix} = \begin{bmatrix} \lambda_1&0&\cdots&0\\ 0&\lambda_2&\cdots&0\\ \vdots&\vdots&&\vdots\\ 0&0&\cdots&\lambda_n \end{bmatrix} \begin{bmatrix} x_1(t)\\ x_2(t)\\ \vdots\\ x_n(t) \end{bmatrix}.$$
## 微分方程组的解：

微分方程通常写成

$$ F(t,x,x',x'',\ldots,x^{(n)})=0. $$


它的解是一个函数 $x=x(t)$，满足：

1. 在规定的区间内具有足够阶数的导数；
2. 把 $x(t)$及其各阶导数代入方程后，方程对区间内每个 $t$ 都成立。

将矩阵 $\lambda$ 用 $A$ 表示，就得到$$\dot{x}(t)=Ax(t)$$
这就是状态方程，我们可以用当前时刻的 $x(t)$ 计算其是怎么变化的（  ）。

d/dt 作用在 x(t) 上效果等同于 x(t) 左z乘 lambda 矩阵

满足这种情况的所有 $x(t) = x(0)e^{\lambda t}$
即x(t) 会落在 $e^{\lambda t}$  张成的空间上（这里有很多lambda,其实lambda构成了一个矩阵）

为什么这是状态方程：我们可以从系统这一刻的状态计算系统是怎么变化的（对t求导）



$$Ax(t)=\lambda x(t)$$
原本我们研究矩阵相似对角化是找一个很棒的坐标系，在这个坐标系下 A 对 x （此时 x 在A所处坐标系下） 的效果等效于一个漂亮的对角矩阵
然后延伸到特征函数上，我们就变成找到一个坐标使得算子的效用在本坐标系下等效于左乘lambda构成的矩阵，考虑研究微分算子，那我们重新整理一下这句话：

我们想找一个坐标使得微分算子在x(t)应用等效于在此坐标下等效于一个线性变换
写出来就是$$\dot{x}(t)=\lambda x(t)$$ 这意味着我们建立了导数和原函数的关系，或者在系统中看，若x(t)代表某个状态（位置、角度），那么其导数（速度，角速度）可以通过lambda矩阵和x(t)得到。
我们建立了状态和其变化的关联。

对此微分方程求解会发现，我们$\frac{\mathrm{d}}{\mathrm{d}t}\left(e^{\lambda t}\right)=\lambda e^{\lambda t}$

线性动态系统中，状态的变化由微分算子描述，而研究微分算子的特征函数，可以找到系统最基本的变化模式。我们注意到存在这样的特征函数$$\frac{\mathrm{d}}{\mathrm{d}t}\left(e^{\lambda t}\right)=\lambda e^{\lambda t}$$
和这样的特征基：$$[e^{\lambda_{1} t},e^{\lambda_{2} t} \dots e^{\lambda_{n} t}]$$
而所有符合条件的状态量x(t)都可以用这个特征基线性表示。这是微分算子本身的性质所致
而若继续研究特征基，我们可以认为状态量能被特征基线性表示意味着复杂的系统状态可以被分解为若干个彼此独立（基）的基本动态模式（指数动态模式）


## 状态方程

刚才得到$$\dot{x}(t)=Ax(t)$$
这里还没有输入，则加上 $Bu(t)$ 得到$$\dot{x}(t)=Ax(t)+Bu(t)$$
即系统在输入 $u(t)$ 的作用下，变量 $x(t)$ 是如何变化的

对一个系统，不是所有的状态都可以被观测，所以引入输出方程（输出意味着系统输出的能被观测的部分）$$y(t)=Cx(t)+Du(t)$$
这里的 Du(t) 是输入里不经过系统状态就直接影响输出的量。就比如说如果用一个固定的值进行重力补偿云台，那么这个补偿量可以被Du(t)通道描述
