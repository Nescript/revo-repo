I install several packages through the intro on [https://pi.dev/packages]
```bash
pi install npm:pi-mcp-adapter
pi install npm:@juicesharp/rpiv-ask-user-question
pi install npm:pi-web-access
```
as a multi device person, i should sync those setups through some way, may a git repo for it

update at 2026/6/28: now i know i can simply use git to sync my pi config. It will automatic install skill and packages.

I mainly use deepseek v4-flash as pi's default model. It turns out it work pretty well on those basic jobs. as a assisstent it is cheap and envience enough.