1. 进制转换
小数点后的部分使用连乘法
图片展示例子：
二进制转八进制，十六进制可以3(4)位分组转换，反过来同理，1对3(4)

码制中原码的符号位
放在最高位，0表示正，1表示负

反码
正数的反码就是原码
负数的反码就是除了符号位按位取反

补码
正数的补码与原码相同
负数的补码就是反码+1

# 编码
顺序二进制编码（建成二进制码）
就是十进制数转换成二进制数所得到的二进制编码
缺陷是：考虑7-8(0111-1111)，四位都发生了跳变，可能会出现短暂的1111、1011，可能导致严重的电路状态错误，进而进出以下编码

格雷码（循环码，发射码）
特点是相邻两个编码只有一个不同

独热码
00001
00010
00100
01000
10000
适合做状态机

BCD码
就是位权写出来的二进制编码
如8421BCD码(基本就是二进制了)

ASCII 码
老熟人了


# 逻辑代数
是分析设计逻辑电路的数学工具
 参与逻辑运算的变量用大写字母表示，称为逻辑变量
 每个变量取值：0 or 1

与逻辑 全真则真 $Y=A \cdot B$

或逻辑，一真则真 $Y=A+B$

非逻辑 $Y=\bar{A}$ 取反

这三个组合又得到：
与非 全真才假 $Y=\bar{AB}$
或非 才真全假 $Y = \bar{A+B}$
异或（不可同）$Y=A\oplus B$
同或（相同则真）$Y=A\odot B=\bar{A}\bar{B}+AB$ aka异或非


逻辑运算的公式与定理
常量之间 ai你来补充
常量与变量之间 ai来补充 e.g.  $A \cdot 0=0$

定理 交换律 结合律 分配律  

需要严肃关注补充 一些常用公式part

逻辑表达式 用逻辑运算表示各个变量之间逻辑关系的代数式子，称为逻辑表达式  
表示方式：真值表 卡诺图
卡诺图是将真值表的每一种变量取值组合对应的函数值添入方格中

逻辑图是用图形方式表示逻辑函数，譬如之前与门或门非门
