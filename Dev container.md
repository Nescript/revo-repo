相关 RAW
[[devcontainer-ssh-forwarding-agent-development-dotfiles-best-practices]]
[[vscode-devcontainer-dotfiles-suitability]]
我可以通过Dev container来统一我的容器内开发环境

例如 oomwoo 提供 ros 环境容器，可以先依其构建一个 Dockerfile 安装一些基本配置（包括dotfiles）然后工作空间也需要挂载

也许dev container 可以提前配置好vscode的插件（maybe by dotfile）

